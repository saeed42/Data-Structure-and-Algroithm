#include <stdio.h>

int main()
{
  int i, n, num;
  
  scanf("%d", &num);
  
  int arr[num];
  
  //input array element
  for(i = 0; i < num; i++) {
      scanf("%d", &a[i]);
  }
  
  
  //print array element
  
  for(i = 0; i < num; i++)  {
      printf("%d\n", a[i])
  }
  
  return 0;
  
}
